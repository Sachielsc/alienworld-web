`doc\about AI orchestration system for our current project.md` is the plan to create a AI orchestration system for our current project "Charles' Alien World"'s frontend beautify. please note that this plan is NOT a final version. and here is my ideas of modification:
1. i will use Claude Code as our Orchestrator, code writter and feature/frontend design implementor, as I have pro subscription of Claude Code so we should have enough quotas. And Claude Code is the only LLM that have permission to code writing
2. i will use Gemini (or other LLM, configurable) as our UI/UX Designer. but since i may only have free tier api key for this LLM, so it is up on orchestrator's opinion that whether we need to deliever the UI/UX Designer work to Gemini (or other LLM, configurable)
3. i will use GPT(or other LLM, configurable) as our Senior Reviewer. but since i may only have free tier api key for this LLM, so it is up on orchestrator's opinion that whether we need to deliever the UI/UX Designer work to GPT(or other LLM, configurable)
4. since all the other LLMs i only have free tier, so when orchestrator decides to hand over the work but then find out that LLM connection is unsuccessful, then the orchestrator (Claude Code) will just do the work itself
5. UI/UX Designer's role and work is about using a browser mcp like playwright to navigate to the specific page we trying to beautify, and give some professional suggestion about the UI design
6. Senior Reviewer's role and work is about reading the frontend code and relevant backend code of the specific page we trying to beautify, can give some code review suggestion about coding syntax, structure and bug check.

now discuss this whole plan with me before you start to build this automatic orchestration workflow